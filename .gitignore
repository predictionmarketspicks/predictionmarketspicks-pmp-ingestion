node_modules
.env
.env.local
*.pem
*.key
.DS_Store
.vscode
.idea
dist
coverage

# External-benchmark feed captures are INTERNAL-ONLY raw vendor data — never
# commit them. Only the *.example.json shape templates are tracked.
data/ext-staging/*.json
!data/ext-staging/*.example.json

# env backups carry live creds — never trackable (WEEK1 F10, 2026-08-31)
.env.bak*
